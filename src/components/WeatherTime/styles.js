import {StyleSheet} from 'react-native';

export default StyleSheet.create({
    container: {
        // flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
  WeatherIcon: {
    width: 50,
    height: 50,
  },
  text:{
        color: '#fff',
        fontSize: 14,
  }
});
