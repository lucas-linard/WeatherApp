import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        padding: 10,        
        flexDirection: 'row',
        justifyContent: 'space-between',        
    },
    minText: {
        paddingHorizontal: 2,
        color: '#808080',
        fontSize: 20,
    },
    maxText: {
        paddingHorizontal: 3,
        color: '#fff',
        fontSize: 24,

    },
    
});